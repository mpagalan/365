$("div.fullimage:not('#default_image'):last")
  .after("<%= escape_javascript(render( :partial => 'pages/pages_image', :locals => {:pages => @pages})) %>");
$(window).ready(function(){
  refigure();
});
